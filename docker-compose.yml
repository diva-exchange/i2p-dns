version: "3.9"

name: divaDns
services:
  webapi:
    container_name: webapi
    build:
      context: ./src/DivaDnsWebApi
      dockerfile: ./Dockerfile
    ports:
      - "5000:80"
      - "5001:443"